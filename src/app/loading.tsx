/**
 * @fileoverview 全局加载页面
 * @description 根布局的加载状态展示
 * @module app/loading
 */

import { PageLoader } from '@/components/ui/PageLoader';

export default function Loading() {
  return <PageLoader />;
}
